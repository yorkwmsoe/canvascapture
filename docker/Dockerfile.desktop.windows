# This file is used to build the Windows Docker image for the desktop application.
FROM mcr.microsoft.com/windows:1809

# Install Rust
RUN winget install -e --id Rustlang.Rust.GNU
RUN rustup -V
RUN rustc -V
RUN cargo -V

# Install Node.js
RUN winget install -e --id OpenJS.NodeJS.LTS
RUN node -v