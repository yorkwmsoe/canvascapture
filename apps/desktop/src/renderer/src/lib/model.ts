import { Modal } from 'antd'

const { confirm, error, info, success, warning } = Modal

const popConfirm = confirm
const popError = error
const popInfo = info
const popSuccess = success
const popWarning = warning

export { popConfirm, popError, popInfo, popSuccess, popWarning }
