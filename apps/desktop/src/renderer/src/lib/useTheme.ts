/**
 * Define useTheme, used to get the current context design token
 *
 * See the definition below for more details
 */
import { theme } from 'antd'

export const useTheme: typeof theme.useToken = theme.useToken
