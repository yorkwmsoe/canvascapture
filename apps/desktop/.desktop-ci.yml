image: node:18

stages:
  - install
  - test
  - build
  - release

variables:
  BASE_PATH: apps/desktop

cache:
  paths:
    - $BASE_PATH/node_modules/

before_script:
  - cd $BASE_PATH/

install-desktop:
  stage: install
  script:
    - npm ci

test-desktop:
  stage: test
  needs: ['install-desktop']
  script:
    - npm run test
  artifacts:
    when: always
    reports:
      junit:
        - $BASE_PATH/junit.xml
  coverage: '/All files[^|]*\|[^|]*\s+([\d\.]+)/'

build-desktop:
  stage: build
  needs: ['install-desktop']
  script:
    - npm run build
  artifacts:
    paths:
      - $BASE_PATH/build/

release-desktop:
  stage: release
  needs:
    - job: build-desktop
      artifacts: true
  script:
    - npm run tauri build
  artifacts:
    paths:
      - $BASE_PATH/src-tauri/target/release/
